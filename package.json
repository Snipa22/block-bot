{
  "name": "block-bot",
  "version": "0.0.1",
  "description": "Block reporting block for Monero pools that implement the standard API's",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Blair",
  "license": "MIT",
  "dependencies": {
    "irc": "^0.5.2",
    "request-json": "^0.6.2"
  }
}
